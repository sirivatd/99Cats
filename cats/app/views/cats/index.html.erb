<h1>All cats!</h1>
<a href="<%= new_cat_url %>">Create New Cat</a>
<ul>
  <% @cats.each do |cat| %>
  <li>
    <%= cat.name %>
    <br>
    <a href="<%= cat_url(cat) %>">Show Cat</a>
  </li>
  <br>
  <% end %>
</ul>
